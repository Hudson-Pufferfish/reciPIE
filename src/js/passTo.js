const titleEl = document.querySelector('.recipe__title');
const title = titleEl.innerText;
const curUrl = window.location.href;

const parentEl = document.querySelector('.upload');
// parentEl.addEventListener('submit', function)
